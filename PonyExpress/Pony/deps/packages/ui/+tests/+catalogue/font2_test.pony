use "yoga"


actor Font2Test is Controllerable
  
	fun ref mainNode():YogaNode iso^ =>
    let font:Font = Font(TestFontJson())
    //let text = "Lorem"
    let text = LoremText()
    
    recover iso 
      YogaNode.>alignItems(YGAlign.flexstart)
              .>flexDirection(YGFlexDirection.row)
              .>flexWrap(YGWrap.wrap)
              .>padding(YGEdge.all, 12)
              .>view( Color.>color(RGBA(0.98,0.98,0.98,1)) )
              .>addChildren( [
                                                        
          // lorem ipsum in 4 quadrants
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>red().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(128).>sizeToFit().>center() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>green().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(16).>sizeToFit().>right() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>blue().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(32).>sizeToFit().>top() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>yellow().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(64).>sizeToFit().>bottom() ) )
          
          
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>yellow().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(64).>sizeToFit().>center() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>red().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(128).>sizeToFit().>right() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>green().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(16).>sizeToFit().>top() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>blue().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(32).>sizeToFit().>bottom() ) )
          
          
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>blue().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(32).>sizeToFit().>center() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>yellow().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(64).>sizeToFit().>right() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>red().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(128).>sizeToFit().>top() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>green().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(16).>sizeToFit().>bottom() ) )
          
          
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>green().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(16).>sizeToFit().>center() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>blue().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(32).>sizeToFit().>right() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>yellow().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(64).>sizeToFit().>top() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>red().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(text, font).>size(128).>sizeToFit().>bottom() ) )
                    
        ]
      )
    end
