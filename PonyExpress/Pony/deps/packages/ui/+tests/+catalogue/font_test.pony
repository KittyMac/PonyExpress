use "yoga"


actor FontTest is Controllerable
  
	fun ref mainNode():YogaNode iso^ =>
    let font:Font = Font(TestFontJson())    
    recover iso 
      YogaNode.>alignItems(YGAlign.flexstart)
              .>flexDirection(YGFlexDirection.row)
              .>flexWrap(YGWrap.wrap)
              .>padding(YGEdge.all, 12)
              .>view( Color.>color(RGBA(0.98,0.98,0.98,1)) )
              .>addChildren( [
                                                        
          // lorem ipsum in 4 quadrants
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>red().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>green().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>blue().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>yellow().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
          
          
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>yellow().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>red().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>green().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>blue().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
          
          
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>blue().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>yellow().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>red().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>green().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
          
          
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>green().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>blue().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>yellow().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
          YogaNode.>widthPercent(25)
                  .>heightPercent(25)
                  .>clips(true)
                  .>view( Color.>red().>alpha(0.1) )
                  .>view( Scroll.>horizontal(false) )
                  .>addChild( YogaNode.>view( Label(LoremText(), font).>size(12).>sizeToFit() ) )
                    
        ]
      )
    end
